# bergamot-py310-mozilla

Build Mozilla Firefox Translations inference engine (bergamot-translator v0.6.0) as a Python wheel for Windows Python 3.10.

Uses Mozilla's marian-fork with Chinese model compatibility fixes.
